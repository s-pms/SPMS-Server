spring:
  jpa:
    show-sql: false
    hibernate:
      # 生产模式请严格使用 validate
      ddl-auto: update
  datasource:
    url: "jdbc:mysql://localhost:3306/dev_spms?allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false"
    username: "root"
    password: "asdfew12345"
  data:
    redis:
      host: "redis"
      port: 6379
      database: 5
app:
  upload-platform: local
  login-url: "https://spms.hamm.cn/login"

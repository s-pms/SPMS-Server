spring:
  jpa:
    show-sql: false
    hibernate:
      # 生产模式请严格使用 validate
      ddl-auto: update
  datasource:
    url: "jdbc:mysql://spms-mysql:3306/spms?allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false"
    username: "spms"
    password: "spms"
  data:
    redis:
      host: "spms-redis"
      port: 6379
      database: 5
app:
  login-url: "https://spms.hamm.cn/login"

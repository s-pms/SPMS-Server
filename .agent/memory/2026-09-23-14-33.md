# 2026-09-23 14:33 会话摘要

## 任务目标
为 SPMS Server 项目生成详细的 AGENTS.md（项目介绍）与 DEV.md（开发指南）。

## 完成内容
- `/Users/hamm/Git/spms/SPMS-Server/AGENTS.md`（14 KB）：项目定位、技术栈、12 个业务模块、系统能力、项目结构图、运行部署、关键设计点、AI 协作建议
- `/Users/hamm/Git/spms/SPMS-Server/DEV.md`（23 KB）：开发指南，启动流程、目录结构、BaseEntity/Service/Controller 扩展点、单据开发四件套、状态机、CodeRule、新建模块流程、IoT 数据上报、WebSocket、OAuth2、MCP、Cron、调试排错、最小可运行单元示例
- 创建 `.agent/memory` 目录并写入本次会话摘要

## 关键决策
- 文档内容完全基于实际代码扫描，未引入未经证实的约定
- AGENTS.md 突出"是什么 / 能做什么"，DEV.md 突出"怎么改 / 怎么扩"
- 单据部分以 InputService、OrderService 为典型，覆盖 audit/reject/addFinish 完整链路
- 包含 BaseController/BaseBillController 默认 exclude 的接口、DevData 初始化顺序、isRootUser 判定等隐藏约束
- 列举所有 XxxServices 服务定位器与对应入口，避免新人误用

## 遗留问题
- 项目原 `README.md` 中提到 `AGENT.md`（无 S），与新建 `AGENTS.md`（带 S）并存。如需统一，需用户确认后修改 README 引用
- `docs/DEV.md` 为空文件，是否需要清理或写入占位说明，待用户决定

## 用户偏好
- 默认使用中文回复
- 代码注释默认不添加
- 不主动提交代码、未要求时不创建 PR